export enum Role {
    Admin = "Admin",
    User = "User",
    SuperAdmin = "SuperAdmin",
}

export enum Sections {
    Products = "products",
    Customers = "customers",
    Categories = "categories",
    Sales = "sales",
}

export enum Operations {
    Create = "create",
    Delete = "delete",
    Edit = "edit",
}
